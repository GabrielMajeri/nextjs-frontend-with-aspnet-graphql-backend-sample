import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable the React Compiler for automatic memoization and other optimizations
  reactCompiler: true,
};

export default nextConfig;
